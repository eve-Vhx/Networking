# Given a CSV file with the following columns:
# Timestamp, Network Info, Signal Info, Signal Quality
# Create a new CSV file with the following columns:
# Timestamp, servingcell_RSSI, RSSI, RSRP, RSRQ

